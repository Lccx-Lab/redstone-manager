-- 第三者提供版ではゲームログイン情報（ID・パスワード等）の保存機能を廃止する。
--
-- !! 破壊的変更 !!
-- このマイグレーションを実行すると account_credentials テーブルと、
-- そこに保存されていた（暗号化済みの）ログインID・パスワード・二次パスワード・
-- 生年月日・登録メールアドレスがすべて削除される。元に戻せないため、
-- 必要なら事前に該当データを別途控えておくこと。

drop table if exists account_credentials;
